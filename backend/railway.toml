[build]
commands = [
  "pip install -r requirements.txt",
  "cd backend/native && python setup.py build_ext --inplace"
]

[start]
cmd = "uvicorn app:app --host 0.0.0.0 --port 8000"

[variables]
PYTHON_VERSION = "3.11"
