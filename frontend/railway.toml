[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npm run preview -- --host 0.0.0.0 --port 4173"

[variables]
NODE_ENV = "production"
VITE_API_BASE_URL = "https://Hilbert-Information-Laboratory.up.railway.app"
